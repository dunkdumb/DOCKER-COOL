version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nri-christian-matrimony
    ports:
      - "5001:5001"
    environment:
      - NODE_ENV=production
      - PORT=5001
      - DATABASE_URL=postgresql://neondb_owner:npg_YRJmU1MteV4x@ep-wandering-violet-ai6hty2k-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require
      - SESSION_SECRET=nri-christian-matrimony-super-secret-key-2024
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5001/api/health', (r) => { process.exit(r.statusCode === 200 ? 0 : 1) }).on('error', () => process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
